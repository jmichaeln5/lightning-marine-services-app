<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Lightning Marine Services App</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">

    <ul class="navbar-nav mr-auto">
      <!-- <li class="nav-item active">
        <%= link_to "About", pages_about_path,class:"nav-link"%>
      </li> -->
      <li class="nav-item">
        <%= link_to "Purchasers", user_purchasers_path(current_user),class:"nav-link" if current_user %>
      </li>
      <li class="nav-item">
        <%= link_to "Vendors", vendors_path ,class:"nav-link" if current_user %>
      </li>
      <!-- <li class="nav-item">
        <%= link_to "Landing Page", pages_landing_page_path,class:"nav-link"%>
      </li> -->
    </ul>

    <div class="navbar-text">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class:"nav-link" unless current_user  %>
          <% if user_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class:"nav-link" %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to "Register", new_user_registration_path, class:"nav-link" unless current_user  %>
          <%= link_to "Account Settings", edit_user_registration_path, class:"nav-link" if user_signed_in? %>
        </li>
      </ul>
    </div>
  </div>
</nav>
